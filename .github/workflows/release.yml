name: Release candidate

on:
  workflow_dispatch:
    inputs:
      version:
        description: Candidate version to verify
        required: true
        default: 0.1.0

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-05-19
      - name: Match candidate metadata
        shell: bash
        run: |
          test "$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)" = "${{ inputs.version }}"
          grep -qx 'publish = false' Cargo.toml
      - run: cargo fmt --all --check
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - run: RUSTDOCFLAGS='-D warnings' cargo doc --all-features --no-deps
      - run: cargo test --all-features
      - run: cargo package --list
      - run: cargo check --all-features

# Publishing is intentionally absent. After the first manual release and
# crates.io trusted-publisher registration, a separate reviewed change may add
# an OIDC publishing job.
